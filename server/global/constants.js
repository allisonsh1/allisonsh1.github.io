/**
 * Contains constants for use by other scripts
 */

const UUID_COOKIE = 'uuidToken'; // The cookie ID for the user's UUID

const USER = 0;
const ADMIN = 0; //!! -------> SET TO 1 IN PROD THIS IS JUST SUPER ANNOYING

module.exports = {
    UUID_COOKIE,
    USER,
    ADMIN,
}